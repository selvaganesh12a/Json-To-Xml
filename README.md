# Json-To-Xml
This project is a Java command-line tool that converts JSON to XML. It handles various JSON types—like objects, arrays, and primitives—and maps them to specific XML elements with attributes. Built with Maven,
